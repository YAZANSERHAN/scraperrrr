[project]
name = "job-scraper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "anthropic>=0.40.0",
    "supabase>=2.9.0",
]

[tool.vercel]
entrypoint = "api.scan:handler"
